# Инопоиск Флюс — «Отписаться»

Мини-игра в духе [neal.fun](https://neal.fun). Одна цель — отменить подписку вымышленного
сервиса «Инопоиск Флюс». Между вами и кнопкой — тёмные паттерны: «вы уверены?», скидка,
опрос из 6 вопросов, чат-бот, капча, таймер ожидания оператора, головоломки.

Согласились оставить подписку (нажали «остаться», приняли скидку, не сняли галочку) —
игра начинается заново с первого уровня. **Таймер и счётчик кликов при этом не
обнуляются.** В финале — поздравление, «подарок от партнёров» и колесо фортуны.

> ⚠️ Пародия. «Инопоиск Флюс» — вымышленный сервис, совпадения с реальными компаниями
> случайны. Логотипы, шрифты и цвета реальных брендов не используются.

## Стек

Vite + TypeScript, без фреймворка. Деплой — GitHub Pages.

Публичная страница проекта — `vonzvyagin.ru/unsubscribe` (домен + Figma Sites).
На финале кнопка «Поделиться результатом» шлёт туда текст+ссылку через Web Share API.

**Превью ссылки (og:image).** Мета-теги Open Graph / Twitter — в `index.html`.
Картинка `public/og.png` (1200×630), исходник `design/og-card.svg`.
`og:image` и `og:url` — **абсолютные**, сейчас стоят `https://vonzvyagin.ru/unsubscribe/…` —
поправить под фактический путь деплоя. Фавикон — `public/favicon.svg`.
Перегенерация картинки: открыть `design/og-card.svg` (контент в центральной полосе
1200×630 внутри квадрата 1200×1200) и экспортировать/обрезать до 1200×630. Открытый вопрос —
отдать этот путь под лендинг в Figma и встроить игру iframe-ом, либо повесить игру
на домен через GitHub Pages + custom domain.

## Разработка

```bash
npm install
npm run dev      # http://localhost:5173
npm run build    # тайпчек + прод-сборка в dist/
npm run preview  # предпросмотр сборки
```

Без хэша игра открывается с интро-экрана; таймер и клики стартуют по кнопке «Начать отмену».
Открыть `#7` в адресной строке — пропустить интро и начать сразу с 7-го уровня (для отладки).

`preview/index.html` — отдельная play-версия одним файлом на чистом JS (без сборки).
Открывается двойным кликом, используется для быстрого плейтеста и как артефакт.
**Держим её вручную синхронной с `src/`.** Переключатель уровней виден только по `?dev`
в адресе; в консоли всегда доступны `__ipp.goto(n)`, `__ipp.twist(i)`, `__ipp.intro()`.

## Деплой

CI — `.github/workflows/deploy.yml`: push в `main` → GitHub Actions → `npm run build`
(`tsc --noEmit && vite build`) → публикация `dist/` на GitHub Pages. Локально Node не нужен,
всё собирает Action.

Первый раз:

```bash
# репо уже инициализирован (ветка main, всё застейджено)
git config user.name  "Имя"
git config user.email "you@example.com"
git commit -m "Инопоиск Флюс — Отписаться: v1"

git remote add origin https://github.com/<логин>/<репо>.git
git push -u origin main
```

Затем в репозитории: **Settings → Pages → Source: GitHub Actions**. Push запустит workflow;
через 1–2 мин игра будет на `https://<логин>.github.io/<репо>/`.

**Свой домен.** GitHub Pages отдаёт сайт с корня домена/поддомена, произвольный путь
(`vonzvyagin.ru/unsubscribe`) напрямую не поддерживается. Проще всего — поддомен
`unsubscribe.vonzvyagin.ru`:

1. DNS: `CNAME`-запись `unsubscribe` → `<логин>.github.io`
2. Положить `public/CNAME` c одной строкой: `unsubscribe.vonzvyagin.ru`
3. Заменить домен в `og:*` / `canonical` (`index.html`) и в `SITE_URL` игры
   (`src/levels/level-99-fortune.ts` + `preview/index.html`) на `https://unsubscribe.vonzvyagin.ru`
4. После деплоя прогреть превью в Телеграме: отправить ссылку боту `@WebpageBot`

Если первый Action упал красным — это почти всегда ошибка `tsc`; лог во вкладке Actions.

## Структура

```
src/
  main.ts                 точка входа (+ чтение #N из хэша)
  engine/
    types.ts              интерфейс уровня и GameApi (complete / fail / penalize)
    game.ts               интро, последовательность уровней, таймер, клики, экран провала, «повороты»
    twists.ts             сквозные «повороты» между уровнями (обновление, разлогин, тёмная тема)
    sound.ts              синтез звука на Web Audio (click/blip/fail/win/tick/wheelSpin), тумблер
    hud.ts                шапка: врущий прогресс-бар, время, клики, тумблер звука
    dom.ts                хелперы: el(), toast(), confetti(), mmss(), плюрализация
  levels/
    index.ts              порядок уровней (финал — всегда последний)
    level-01-settings.ts  найти «Управление подпиской»
    level-02-cookies.ts   cookie-согласие          · провал: «принять все» 2 раза
    level-03-sure.ts      «вы уверены?»             · провал: «остаться»
    level-04-discount.ts  удержание скидкой         · провал: принять скидку
    level-05-survey.ts    опрос из 6 вопросов
    level-06-chatbot.ts   чат-бот удержания
    level-07-captcha.ts   капча с SVG-картинками    · провал: «позвоните мне»
    level-08-operator.ts  ожидание оператора 20 с   · провал: «обратный звонок»
    level-09-smscode.ts   код из SMS                · провал: галочка «сохранить подписку»
    level-10-puzzle.ts    пятнашки 3×3              · провал: «пропустить»
    level-11-slider.ts    ползунок подтверждения    · провал: «оставить подписку»
    level-12-terms.ts     условия + тест на двойные отрицания (отметить 3 из 6) · провал: «формулировки запутаны»
    level-13-maze.ts      лабиринт (pointer-трейс)  · провал: «не получается мышкой»
    level-14-unbundle.ts  отключить 4 услуги по одной · провал: «оставить подписку»
    level-15-founder.ts   входящий звонок Аркадия, плохая связь, только субтитры · провал: «отклонить»/«завершить вызов»
    level-16-downgrade.ts понижение тарифа по шагу, модалка с путающими кнопками · провал: «подождать»/крупная кнопка «сохранить»
    level-17-handwriting.ts рукописное слово на canvas · провал: «не хочу писать от руки»
    level-18-hold.ts      удержать кнопку 10 с      · провал: «тяжело держать»
    level-19-timing.ts    остановить бегунок в зоне ×3 · провал: «слишком быстро»
    level-20-almost.ts    фейковый успех → ошибка → последний шаг · провал: «вернуться» / галочка
    level-99-fortune.ts   финал: колесо фортуны + кнопка «поделиться» (Web Share API)
  styles/
    tokens.css            палитра, светлая/тёмная тема
    app.css               компоненты и экраны
```

## Как добавить уровень

1. Создать `src/levels/level-NN-name.ts`, экспортировать объект `Level`.
2. `mount(root, game)` рендерит DOM в `root`. Вызвать `game.complete()` — перейти дальше;
   `game.fail(текст)` — показать экран провала и рестарт (таймер/клики сохраняются);
   `game.penalize(мс)` — штраф ко времени.
   Вернуть функцию очистки, если ставите таймеры/слушатели.
3. Добавить в массив `levels` в `src/levels/index.ts` перед `ending`.
4. Продублировать логику в `preview/index.html`.

Готча: глобального `[hidden]{display:none}` в проекте нет. Если прячете элемент через
`el.hidden`, не задавайте ему `display:` в базовом правиле — используйте
`.foo{display:none} .foo:not([hidden]){display:flex}`.

## Осталось по плану

20 уровней + финал + «повороты» + звук готовы. Дальше — финализация и деплой.

**Звук** (`src/engine/sound.ts`) — всё синтезируется осцилляторами, ассетов нет.
Глобальный тик на клики по кнопкам/ссылкам; `blip` на переход уровня и сообщение Стеши;
`fail` на провал; `win` + `wheelSpin` на финале; `tick` на последних секундах у оператора.
Тумблер 🔊/🔇 в HUD, выбор в `localStorage["ipp-sound"]`, **по умолчанию ВКЛ** (тихо,
master 0.14). Поменять дефолт — `readEnabled()` в `sound.ts`.

**«Повороты»** (`src/engine/twists.ts`) — короткие экраны между уровнями по `before`-индексу:
перед L5 «приложение обновилось», перед L10 «сессия завершена, войдите снова» (+штраф),
перед L13 «включили тёмную тему», перед L16 тема возвращается. Показываются один раз;
при провале `data-theme` сбрасывается.

В `preview/index.html` есть временный переключатель уровней (`<details class="lvlnav">`,
помечен «в релизе убрать») — вырезать при финализации. Дев-хелперы: `__ipp.goto(n)`,
`__ipp.twist(beforeIndex)`.
